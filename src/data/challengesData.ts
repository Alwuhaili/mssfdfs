import { InteractiveChallenge } from '../types';

// Operational challenges are created by authorized users and persisted in Firestore.
// No bundled/default competition data is used at runtime.
export const INITIAL_CHALLENGES: InteractiveChallenge[] = [];
